"use server";

// Placeholder for future settings CRUD
// Settings will be stored in a key-value table

export async function getSettings() {
  return {};
}

export async function updateSetting(key: string, value: unknown) {
  return { success: true };
}
